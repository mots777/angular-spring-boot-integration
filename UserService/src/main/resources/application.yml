spring:
  data:
    mongodb:
      authentication-database: admin
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASSWORD}
      database: ${MONGO_DATABASE}
      port: ${MONGO_PORT}
      host: ${MONGO_HOST}
#      username: mothi
#      password: root
#      database: userDb
#      port: 27017
#      host: localhost
  application:
    name: user-service
server:
  port: 8080



